@{
    ViewData["Title"] = "Erişim Reddedildi";
}

<div class="container mt-5 text-center">
    <div class="card shadow-sm border-danger">
        <div class="card-body">
            <h1 class="display-4 text-danger">
                <i class="bi bi-exclamation-triangle-fill"></i> Yetkisiz Erişim
            </h1>
            <p class="lead">Bu sayfayı görüntülemek için yetkiniz bulunmamaktadır.</p>
            <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">Ana Sayfaya Dön</a>
        </div>
    </div>
</div>